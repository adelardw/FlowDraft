# FlowDraft — журнал сессии 5–7 августа 2026

Передаточный документ. Ветка `dev/ys`, стенд SmolLM2-135M, цель — дизайн
функций потерь под многошаговую генерацию и сравнение с бейзлайном Orthrus.

Читать сверху вниз необязательно. Если нужно быстро: раздел «Итог», затем
«Баги» и «Что делать дальше».

---

## Итог одной таблицей

| утверждение | статус |
|---|---|
| Многошаговость требует члена самокоррекции (обучения против вердикта AR на собственный черновик) | **устояло**: `+0.5…+1.3` токена, `t = 3.5…4.9`, два сида × два горизонта, разброс контраста между сидами 0.03–0.07 |
| Многокомпонентной цели нужен более длинный горизонт | **устояло**: у бейзлайна пик на 10000 из 12000 на обоих сидах, у многокомпонентных — на горизонте; ранжирование между 6000 и 12000 поменялось |
| Многошаговый Orthrus («склейка») лучше бейзлайна | **опровергнуто**: эффект разложился на утечку (+0.12), отсутствие весов у бейзлайна и заниженную ошибку |
| Карта потока превосходит Orthrus | **не показано** ни по одной оси |
| Члены CFM несут траекторность | **не разрешено**: знаки переворачиваются между сидами |
| `TPF > 1` значит «быстрее AR» | **зависит от стека**: на eager Qwen3-1.7B `TPF = 2.14` при ускорении `0.918`; на sdpa SmolLM2 обученный драфтер даёт `1.50×` |

---

## Хронология

### Фаза 1. Разбор и дизайн (5 авг, утро)

| кто | о чём | что вышло |
|---|---|---|
| Пользователь | Проанализировать проект. Слабые результаты в ветке vcstk. Нужна многошаговость, не `π_{0,1}`. Учитывать принятые/отвергнутые токены от AR. Валидировать решения критиками | задача |
| Я | Прочитал код, конфиги, INSIGHTS.md, память, замеры в `.work/` | диагноз: все учительские члены целятся в `p_AR(·|корпус)`, значение которого состояние драфтера не меняет |
| Я | Написал `design-multistep.md`: C1 он-полиси таргет, C2 перенос хвоста между циклами, C3 полугрупповая консистентность | дизайн на критику |

### Фаза 2. Четыре критика (5 авг, день)

| критик | приговор |
|---|---|
| теория | C1 **не** уничтожает вырожденный минимум (минимизатор константен по `x₀` и при он-полиси таргете); C3 — его неподвижная точка есть `A_n = A_1`, то есть он **запрещает** цель; C2 — знак механизма перепутан, перенос садится в начало блока, а не в конец |
| новизна | C1 — полный прайор арт (DistillSpec, Draft-OPD, Spec-AUF); C3 — полный прайор арт (FMLM 2602.16813); C2 — частичный, защитима только склейка непрерывного времени с лосслесс-циклом |
| метрики | спаривание по промпту даёт 10× по мощности; порог окупаемости `A₂/A₁ ≥ 1.66` почти не зависит от `K`; перенос и попозиционное время сконфаундены в одном флаге |
| код | 8 DF-форвардов на шаг против 1 у бейзлайна; `_onpolicy_kl` считает вердикт по протёкшему черновику; секции `decode:` нет в train.yaml, поэтому валидационный декод игнорирует то, чему учит прогон |

**Что я сделал по итогам.** Выкинул C3 целиком. Переформулировал C1: не «уничтожает
вырожденный минимум», а «вход содержит частичное вычисление таргета, поэтому при
ограниченной ёмкости чтение входа окупается». Реализовал `weights='ar'` +
`causal_in_block` в адаптере (проверено побитово против обычного каузального
форварда), `_verifier_response`, `_greedy_verdict`, `_selfcorrect_kl`.

### Фаза 3. Первый замер и разворот (5 авг, вечер)

| кто | о чём | что вышло |
|---|---|---|
| Пользователь | «Ты вернул одношаговость. Прыжок только в (0,1)» | справедливо: мой член обучал одну лишнюю пару `(0.5,1)`, при `n=3,4` супервизии не было |
| Я | Переписал `_selfcorrect_kl` на **расписание рестартов** `selfcorrect_rounds` | взял у выкинутого `rollout_kl` развёртку по расписанию, заменив корпусный таргет на он-полиси |
| Я | A/B на SmolLM2-135M, 400 шагов | знак перевернулся: `A₂/A₁` 0.34 без члена против 1.28 с ним |
| Я | Обнаружил, что декод стохастичен — один чекпоинт даёт разброс до 0.16 токена | написал `measure.py`: `fixed_prior` + несколько сидов, единица наблюдения (промпт, сид) |

### Фаза 4. «Надо превзойти Orthrus» (5 авг, ночь)

Head-to-head показал, что Orthrus впереди. Убрал в корзину отвергнутое:
`rollout_kl`, `ar_kl_weight`, `onpolicy_kl` с переносом, `frozen_state`,
«точный таргет» `teacher_source=onpolicy` (`−0.155`, `t = −6.0`), полугрупповой
член. Конфиг с 230 строк до 106.

**Затем нашёл, что бейзлайн вообще никогда не обучался.** `Orthrus._masked_kl_chunked`
нормируется через `float64`, которого на MPS нет; падал на первом шаге. Lightning
писал `last.ckpt` при разрушении, а мой харнесс считал руку обученной по наличию
файла. Все дневные сравнения были против головы на `global_step = 0`.

Починил: `_accum_dtype` по устройству; охрана Qwen3 только на CUDA-путь (плотная
маска работает на любом бэкбоне); `shuffle_buffer` 1000 → 64; проверка по
`global_step`; готовность по чекпоинту, а не по выходу процесса (он виснет в
деструкторе pyarrow **после** успешного завершения).

### Фаза 5. Горизонт (6 авг)

| кто | о чём | что вышло |
|---|---|---|
| Пользователь | «Многокомпонентным нужно дольше учить. Смотри кривые» | подтвердилось: контраст прошёл `−0.034 → +0.095 → +0.105` на 2000/4000/6000 |
| Я | README с формулами, пятью фигурами, таблицей «где что пишется» | раздел «SmolLM2-135M bench» |

### Фаза 6. Слепой критик (6 авг, вечер)

Дал критику дизайн и протокол **без чисел**, потребовал предсказаний со знаками.
Он угадал почти всё и нашёл шесть дефектов измерения. Главный:

**У маскирующих рук пять декодных сидов дают побитово одинаковые ячейки** — их
декод детерминирован, приор не участвует. Значит независимых наблюдений 24, а не
120, и стандартная ошибка занижена в `√5 ≈ 2.24` раза. После кластеризации три
из четырёх «установленных» выводов рассыпались.

Плюс: веса позиций стояли только у симплексных рук; `fixed_prior` не покрывал
розыгрыш рестарта; возобновление пересобирало косинус и подбрасывало LR к 79%
пика; утечка в маскирующей самокоррекции; рассогласование доли коммита.

### Фаза 7. Переобучение всего (6–7 авг)

Шесть рук × два сида × 6000 шагов, затем семь рук × 12000 с отбором по
`val/tpf`, затем второй сид на 12000. Всё с исправленным кодом.

**Результаты на 12000 (лучшая точка по `val/tpf`):**

| рука | `A@1` s42/s43 | `A@2` | `A@4` |
|---|---|---|---|
| orthrus | 1.564 / 1.555 | 1.877 / 2.056 | 2.526 / 3.187 |
| flow_sc | 1.615 / 1.743 | 2.126 / 2.117 | 2.544 / 2.574 |
| flow_sc_cfm | 1.495 / 1.612 | 2.073 / 2.046 | 2.514 / 2.558 |

Контроль `orthrus_sc_leaky` (утечка на месте, веса на месте) дал `A@1 = 1.451`
против `1.329` у починенной — то есть **утечка стоила +0.12**, и вчерашнее
преимущество склейки было ею.

### Фаза 8. Критик по теории и статистике (7 авг)

- При двух сидах `df = 1`, `t₁,.975 = 12.71`. Моё «при `n=4` мы значимо хуже»
  (`t = −2.26`) — артефакт наивного объединения; корректно `t = −0.94`.
- «Знак совпал 4 из 4» несостоятельно: разности при `n=1` и `n=2` внутри сида
  коррелируют на `r ≈ 0.8`, независимых испытаний два.
- `TPF@2 > 1`: `t = +0.49` и `+0.39`, интервал вчетверо шире эффекта.
- Лемма о фиксации префикса верна для оператора `T` на токенах, но карта читает
  не токены, а `(1−s)x₀′ + s·q` — при `s = 0.5` и `discunif` argmax входа есть
  **шумовой токен**, и лемме нужна дополнительная посылка, которой в ней нет.
- Насыщенная лемма даёт `TPF = 1` ровно, то есть она — пол, а не механизм
  ускорения.

### Фаза 9. Профилирование (7 авг)

Три негодных замера подряд с моей стороны (некропнутый кэш; необученная модель;
обобщение числа с чужого стека), затем корректный:

| | время | форвардов | `A` | модель / обвязка |
|---|---:|---:|---:|---|
| FlowDraft, 1 прыжок | 978 мс | 53 | 0.85 | 94% / 6% |
| AR | 1462 мс | 48 | — | 97% / 3% |

**Ускорение 1.50×** на SmolLM2-135M/sdpa. Цифра `0.918` из репозитория — с
Qwen3-1.7B на **eager**, где квадратичное внимание наказывает DF-форвард с
восемью позициями против одной. То есть TPF конвертируется в скорость, но
коэффициент зависит от ядра внимания.

---

## Баги

### Найденные и починенные

| баг | где | следствие |
|---|---|---|
| `float64` в нормировке KL | `orthrus.py` | бейзлайн не обучался на MPS вовсе; полдня сравнений с необученной головой |
| охрана Qwen3 на всей блочной ветке | `df_adapter.py` | блочный путь недоступен на llama, хотя плотная маска работает везде |
| проверка успеха по наличию `last.ckpt` | харнесс | Lightning пишет его и при разрушении после исключения |
| готовность по выходу процесса | харнесс | процесс доучивается и виснет в деструкторе pyarrow навсегда |
| SE по 120 ячейкам | `measure.py` | занижение в 2.24 раза, три ложных вывода |
| веса позиций только у симплексных рук | `final_ab.py` | контраст «состояние» содержал контраст «веса» |
| `fixed_prior` не покрывал рестарт | `flowdraft.py` | при `n>1` руки не спарены по шуму второй ноги |
| косинус пересобирался при возобновлении | `longer.py` | LR подскакивал к 79% пика, точка 4000 ущемлена |
| утечка: второй форвард маскирующей ветви двунаправлен | `orthrus.py` | строка, предсказывающая позицию `j`, читала зафиксированный токен из слота `j` |
| доля коммита `K/2` в обучении против `round(K(k+1)/n)` в декоде | `orthrus.py` | обучение не показывало состояние, которое читает декод |
| висячие часы попозиционного времени после удаления переноса | `flowdraft.py` | `carried` без присваивания |
| `setsid` (нет на macOS), `+` в оверрайде существующего ключа, тройная кавычка при генерации файла | скрипты | три потерянных часа на прогонах, которых не было |

### Найденные и НЕ починенные

**1. Позиция разрыва вычёркивается из супервизии.** В маскирующей ветви разрыв
определяется **порядком** (первое расхождение), а `committed` — **уверенностью**
(топ-K по вероятности). Когда разрыв попадает в топ по уверенности, он входит в
`committed`, а супервизируются только `~committed` — то есть единственная
информативная позиция исключается целиком. Это моя же правка утечки, сделанная
слишком грубо.
*Влияние:* маскирующая ветвь на заметной доле блоков получает нулевой полезный
градиент. Требует переобучения `orthrus_sc`.

**2. Рассогласование числа ног.** `selfcorrect_rounds=2` обучает две ноги
рестарта; декод при `n=4` делает **три**, и третья (`s = 0.9`) не обучена вообще.
*Влияние:* ровно там знак контраста переворачивался между сидами. Требует
переобучения всех симплексных рук с `selfcorrect_rounds=3`.

**3. Неравенство доз между ветвями.** Симплексная самокоррекция крутит две ноги и
делает дополнительный AR-свип для второй; маскирующая — один DF-форвард и ни
одного свипа. README называет это «тем же членом» — это не тот же член.
*Влияние:* вывод «непрерывное состояние не несёт нагрузки» из этого контраста не
следует.

**4. Мелочи статистики.** `measure.py` использует 1.96 вместо `t₂₃,.975 = 2.069`
(CI занижен на 5.5%); печатает ~70 парных тестов без поправки на множественность;
свёртка по `PROMPTS` из окружения без проверки, что промптов действительно
столько.

**5. `gamma_clamp: 1.0e-4` ниже машинного эпсилона bf16** (≈8e-3). На CUDA/bf16
при `s → 1` знаменатель округлится в ноль. Конкретный будущий NaN.

**6. Упакованные документы.** `_prepare_blocks` не вызывает
`_packed_document_layout`; `causal_limit = якорь−1` пускает блок ко всему AR-
префиксу через EOS. У обеих рук одинаково, поэтому сравнение не портит, но
абсолютные числа меняет.

---

## Состояние репозитория

```
src/            код: адаптер, три варианта модели, конфиги
results/        4 JSON с ячейками замеров + 5 фигур, на которые ссылается README
bench/          скрипты стенда (в .gitignore, локальные)
bucket/         отвергнутое с обоснованиями (в .gitignore)
checkpoints/    final_ab (2000 шагов), final_long (4-6k), night12k (12000)
```

Коммиты: `73d726b` (бейзлайн стал обучаемым + член самокоррекции + чистка),
`bda9148` (стенд из `.work` в `bench`), `78e288f` (ветка только под проект).

## Что делать дальше

1. **Замерить wall-clock на целевом стеке** (CUDA/bf16, sdpa или flash) для
   `variant=orthrus, jumps=1`. Это решает, конвертируется ли приёмка в скорость
   там, где будет идти настоящий прогон. Час работы, до всякого обучения.
2. **Починить баги 1–2** и переобучить, если нужен честный контраст ветвей.
3. **Промпты прежде сидов.** При `n ≤ 2` внутрисидовая ошибка доминирует над
   межпрогонной: 24 → 200 промптов срежет её втрое и стоит часы. Сиды окупаются
   после этого и только при `n = 4`.
4. **Qwen3-0.6B:** конфига нет, заводить копией `qwen3_1.7b` (не SmolLM2 — у
   Qwen3 есть `q_norm`/`k_norm`, копия от Llama молча выбросит две матрицы на
   слой). `anchors_per_sequence > 1` включает разреженный путь FlexAttention,
   который на MPS не исполнялся ни разу — самый непроверенный код в проекте.
