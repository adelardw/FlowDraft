# Посылки целевой функции

Список того, при чём выводы вокруг лосса верны, и что перестанет быть верным,
если посылка не выполнена. Составлен по результатам разбора математики и
переносимости 7 августа 2026.

**Не всё, что сюда попало изначально, было посылкой.** Четыре пункта оказались
дефектами корректности и починены в тот же день; они оставлены здесь с пометкой
ПОЧИНЕНО, потому что объясняют, почему числа до 7 августа несравнимы с числами
после. Остальные — действительно условия применимости: они говорят, при чём
выводы верны, и кодом не устраняются.

Обозначения: `a_j` — вероятность приёмки позиции `j`; `E[len] = Σ_j Π_{i≤j} a_i`;
`π_{s,t}` — карта потока; `p_AR` — замороженный верификатор.

---

## 1. Веса позиций предполагают позиционно-постоянный коэффициент связи

`position_weights` реализует `∂E/∂a_j = S_{j−1}(1 + R_j)`. Формула проверена
центральными разностями (максимальная ошибка 1.4e-10). Но взвешивать `KL_j` на
`∂E/∂a_j` законно лишь если `∂a_j/∂θ ≈ −κ·∂KL_j/∂θ` с **одним и тем же** `κ` для
всех позиций. У глубоких позиций энтропия таргета выше, значит `κ_j` не
константа, и этого множителя во взвешивании нет вовсе.

*Что ломается:* профиль перераспределяет градиент правильно по метрике и
неправильно по чувствительности. Направление ошибки неизвестно.

## 2. Профиль заморожен, а `∂E/∂a` зависит от точки

Веса посчитаны при фиксированном `a` и по ходу обучения не пересчитываются.
Стационарная точка `Σ c_j·KL_j` при постоянном `c` — стационарная точка
линейного функционала `Σ c_j·a_j`, а не `E`, если только `c` не равно `∂E/∂a`
именно в этой точке. Численно (вогнутая ёмкость, измеренная точка при `a₁=0.6`):
прямой максимум `E = 2.820`, максимум `Σ W_j·a_j` даёт `E = 2.804` — теряется
0.56%. По мере роста `a` истинный `∂E/∂a` выпрямляется, и замороженный профиль
всё сильнее морит голодом глубокие позиции.

*Величина и НАПРАВЛЕНИЕ зависят от того, где закреплён профиль* — записанное
здесь раньше «морит голодом глубокие позиции» верно лишь когда профиль
закреплён НИЖЕ рабочей точки. При закреплении выше он их, наоборот,
перекармливает. Доля массы весов на позициях 8..31 против их истинного вклада
в `E[len]`:

| профиль | масса 8..31 | при `a=0.607` | `0.70` | `0.80` | `0.90` |
|---|---|---|---|---|---|
| 0.607 (замерено на K=8) | 3.0% | 1.0x | 0.4x | 0.1x | 0.1x |
| **0.80 (стоит сейчас)** | 20.5% | 6.8x | 2.5x | **1.0x** | 0.4x |
| 0.935 (опора Orthrus) | 49.0% | 16.1x | 5.9x | 2.3x | 1.1x |

Ошибка в сторону агрессии хуже, чем кажется: приёмка есть конъюнкция, и
улучшение первой позиции умножает все последующие, так что перекорм хвоста
недокармливает ровно те позиции, через которые хвост и достижим.

Попозиционная приёмка логируется на каждой валидации, поэтому после прогона
профиль можно ПОСЧИТАТЬ из данных вместо выбора.

## 3. ПОЧИНЕНО. Профиль был нормирован на `S₁ = 1`, то есть предполагал `a₁ = 1`

`∂E/∂a_1 = 1 + R_1` не содержит `a₁`, тогда как `∂E/∂a_j` при `j ≥ 2` ему
пропорционально. Опубликованный профиль воспроизводит `dE/da` при кривой
выживания, начинающейся ровно с 1.0. При реальном `a₁ = 0.8` истинный вес первой
позиции выше в 1/0.8, при `a₁ = 0.6` — в 1/0.6.

*Что ломалось:* самая ценная позиция систематически занижена в `1/a₁` раз.

**Починка.** `train.acceptance_profile` задаёт `a_j`, вес считается из них
точно; формула сверена с центральными разностями до `1.4e-7`. Прежний жёсткий
список занижал первую позицию в `1/a₁`, то есть в 1.47 раза при измеренном
`a₁ = 0.525`. Сейчас в стенде стоит `[0.8]` — это ПРИОР, а не измерение, см.
пункт 2.
Заодно снят дефект хвоста: при `block_size = 64` позиции 8+ забирали 35.9% всей
массы веса (повтор последнего ВЕСА вместо продолжения выживания), стало 2.8%.

## 4. Один и тот же профиль применяется каждому раунду самокоррекции

Если член работает — если раунд `k` чинит позиции `1..k` — то кривая приёмки
раунда `k` площе, и его `∂E/∂a` тоже. Применение профиля раунда 0 к раунду `k`
неверно, причём в направлении, которое предсказывает сам член.

## 5. Гейт согласованности цепочки точен **поточечно при фиксированном контексте**

Равенство `p_AR(·|корпус) = p_AR(·|жадное продолжение)` до первого расхождения
top-1 доказано индукцией и держится, включая саму позицию разрыва (там префикс
ещё идентичен, загрязняются `i*+1` и дальше). Off-by-one в реализации нет.

*Посылка, которая этим НЕ закрыта:* в декоде весь контекст до якоря — собственное
жадное продолжение модели, а не корпус. Гейт чинит только внутриблочное
обусловливание. Сдвиг распределения контекста между блоками не трогает ни он, ни
что-либо ещё в целевой функции.

## 6. Лемма о фиксации префикса — про оператор на токенах, а карта читает не токены (следствие ПОЧИНЕНО)

Индукция `A_n ≥ min(n, K−1)` для оператора `T(ctx,d)_j = argmax p_AR(·|ctx,d_{<j})`
проходит; единственность неподвижной точки доказана. Но карта читает
`(1−s)x₀′ + s·q`, а не `d`. «Реализует `T` точно» должно держаться **для каждого
розыгрыша приора и каждого `s` расписания**, а черновик восстановим из входа лишь
при `s > 1/(1 + q_max)` — это 0.53 при уверенности 0.9 и 0.83 при 0.2. Отсюда
`selfcorrect_s_min: 0.5`: нижний бин стратификации при `s_min = 0` лежит целиком
ниже порога, и там минимум члена — вход-независимая смесь, то есть константная
карта.

*Следствие, которое надо называть:* насыщенная лемма даёт `TPF = 1` **ровно**.
Это пол, а не механизм ускорения: ускорение требует `A_n > n` строго, а оценка
даёт `A_n ≥ n`. Она же означает, что прыжков больше `K−1` строго хуже AR.

## 7. Разгрузка входа требует насыщающейся области у замороженного ствола

`verify_kl` считается при `s = 0` на случайном приоре, и его таргет от розыгрыша
не зависит — минимум требует `∂π_{0,1}/∂x = 0`, то есть карта обязана вход
игнорировать. Самокоррекция требует `∂π_{s,1}/∂x ≠ 0`. Обусловливание входит
аддитивным позиционно-однородным сдвигом `inputs_embeds + conditioning` с
zero-init выходного слоя; сдвиг не гейтит якобиан. Значит нужна явная посылка: у
замороженного ствола есть насыщающаяся область, достижимая этим смещением.

## 8. Отношение `4·EC + 2·TD` осмысленно только для единичного отрезка времени

`EC` — наты, инвариант к репараметризации `t → φ(t)`. `TD = γ²‖∂_t π‖²` несёт
`1/время²` и масштабируется как `(φ′)⁻²`. Безразмерной пары `(4, 2)`, делающей
сумму однородной, не существует.

Вторично, ПОЧИНЕНО в части метрики: `EC` — кросс-энтропия, не KL, то есть
`CE = KL + H(tgt) > 0`. Член никогда не достигает нуля, а логируемое значение
доминировалось энтропией таргета, поэтому сравнение его масштаба с `verify_kl`
читало константу. Теперь логируется KL (`CE − H(tgt)`); в лоссе остаётся CE,
градиент тот же — `tgt` отцеплен.

## 9. ОТОЗВАНО. Утверждение было ложным

Здесь стояло: «при `t → s` таргет и предсказание приходят из одного вызова сети,
`EC = CE(sg p, p) = H(p)`, и градиент по логитам **не нулевой**, хотя
`p − sg(p) = 0`». Утверждение неверно.

`∂/∂z_k [−Σ_v sg(p_v) log p_v] = −Σ_v p_v(δ_{vk} − p_k) = 0`. Замерено дважды
независимо: `|∂CE/∂logits|_∞ = 2.8e-17` в fp64 и `1.9e-9` в fp32 — машинный
ноль. Для контраста то же выражение БЕЗ отцепления таргета даёт `1.4e-1`, то
есть «заострение уверенности» возникает только если таргет не отцеплен, а он
отцеплен.

**Цена ошибки.** По этому утверждению была введена ручка `train.ec_gamma_min`,
отбрасывавшая розыгрыши с малой гаммой. Она не исправляла ничего: убирала
строки, чей вклад в градиент и так был нулевым, а единственным её эффектом было
домножение члена на `1/P(γ>0.05) = 1.19`. Ручка снята, в коде её нет.

**Что верно вместо этого.** Ровное равенство `EC = CE(sg p, p)` держится только
при `s = t` в точности — мера нуль. При `s < t` таргет есть отдельный форвард
`π_{t,t}(x_jump)`, и градиент пропорционален `t − s`: замерено `1.8e-4` при
зазоре `1e-4` и `3.6e-1` при `0.2`, строго линейно. То есть пары с `s ≈ t` не
вредны, они **слабы**. Поэтому `train.min_jump_gap` оставлен в нуле: он
действует ровно на одну руку из десяти (замер: лосс тождествен при gap 0 / 0.05
/ 0.2 у девяти, `11.564 / 11.530 / 11.286` у десятой), а доказательств пользы
нет.

**Урок, который дороже самого пункта.** Утверждение пришло от критика, было
подкреплено числами и звучало убедительно. Оно продержалось три часа и стоило
правки, которую пришлось откатывать. Находка, не показанная запуском, — гипотеза.

## 10. Член самокоррекции — DAgger, а не оптимизация композиции

`argmax` убивает градиент таргета, `detach` убирает градиент входа. Значит член
не содержит производной «как мой черновик меняет задачу следующей ноги»: это `r`
независимых одношаговых регрессий на он-полиси распределении входов. Само
распределение входов зависит от `θ`, и эта зависимость отброшена, поэтому
суммарный лосс **не является градиентом никакой скалярной функции `θ`** —
потенциала, по которому идёт спуск, нет. Из единственности неподвижной точки
рекурсии сходимость обучения не следует.

## 11. Пороги приора (исправлено 7 августа 2026)

При `V = 151936` чистый токен становится argmax состояния `x_s = (1−s)x₀ + s·x₁`
при:

| приор | порог | примечание |
|---|---|---|
| `dirichlet` | `s > 8.2e-5` | `(ln V + γ)/V`; ранее в комментариях стояло 7.4e-5 |
| `discunif` | `s > 0.5` | **только для one-hot `x₁`** |
| `gaussian` | `s > 1.1e-2` | реализация делит на `√V`; ранее стояло 0.815, что соответствует НЕмасштабированному гауссу |

Для состояния самокоррекции второй аргумент — софтмакс, не one-hot, поэтому порог
`discunif` там равен `1/(1 + q_max − q_r) ≈ 1/(1 + q_max)`, а не 0.5.

## 12. ПОЧИНЕНО. Нормировка гейта была эмпирической и на стенде смещённой

`_position_weights` делит гейт на его среднее по `(batch, anchors)`. При
`batch_size = 2, anchors = 1` это `N = 2`, а `E[1/m̂] ≠ 1/E[m̂]`: на позиции 4
реализованная масса веса 0.241 против намеренных 0.400, на позиции 7 — 0.042
против 0.087, причём с вероятностью 0.945 в батче нет ни одного валидного
сэмпла и гейт тождественно равен единице. При `N = 128` смещение меньше 1%.

*Что ломалось:* на стенде гейт вдвое резал ровно те глубокие позиции, которые
брался защищать.

**Починка.** Знаменатель — скользящее среднее по шагам, а не среднее по текущему
батчу, то есть оценка по тысячам наблюдений вместо двух. В чекпоинт не
сохраняется и прогревается заново примерно за сотню шагов после возобновления.

*На наших данных правка инертна:* среднее хранится по ширине блока, а ширина за
40 шагов ни разу не сменилась (31 во всех 120 вызовах) — самая короткая строка
потока 149 позиций при нужных 33. Правка нужна там, где строки короче.
