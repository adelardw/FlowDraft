# @package _global_
# Непрерывное состояние плюс многошаговость — главный контраст.
# Зеркало `smollm_flowdraft_multistep`.
defaults:
  - /experiment/qwen06_base
  - _self_

output_dir: checkpoints/qwen06_flowdraft_multistep
train:
  variant: flowdraft_block_wise
  prior_type: discunif
  verify_kl_weight: 1.0
  endpoint_weight: 0.0
  lambda: 0.0
  terminal_time_fraction: 1.0
  selfcorrect_kl_weight: 1.0
  selfcorrect_rounds: 2
  selfcorrect_s_min: 0.5
  selfcorrect_tail_weight: 0.5
