# @package _global_
# Task stage 5 ablation: the AR-teacher term ALONE (consistency off).
# Expectation: the diagonal learns, jumps get no training signal.
#   uv run python src/train.py +experiment=ablate_teacher_only
defaults:
  - override /model: qwen2_0.5b

output_dir: checkpoints/ablate-teacher

train:
  variant: fixed
  lambda: 0.0
  checkpoint_name: ablate-teacher-{step:07d}
