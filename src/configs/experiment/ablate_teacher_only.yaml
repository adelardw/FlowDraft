# @package _global_
# Task stage 5 ablation: categorical endpoint inference ALONE (consistency off).
# Expectation: the diagonal learns, jumps get no training signal.
#   uv run python src/train.py +experiment=ablate_teacher_only
defaults:
  - override /model: qwen2_0.5b

output_dir: checkpoints/ablate-endpoint

train:
  variant: fixed
  lambda: 0.0
  checkpoint_name: ablate-endpoint-{step:07d}
