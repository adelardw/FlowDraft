# @package _global_
# Непрерывное состояние без многошаговости — абляция. Зеркало `smollm_flowdraft`.
defaults:
  - /experiment/qwen06_base
  - _self_

output_dir: checkpoints/qwen06_flowdraft
train:
  variant: flowdraft_block_wise
  prior_type: discunif
  verify_kl_weight: 1.0
  endpoint_weight: 0.0
  lambda: 0.0
  terminal_time_fraction: 1.0
