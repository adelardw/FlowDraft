# @package _global_
# Brief step 1: reproduce the Orthrus baseline at tractable scale.
# 0.5B backbone, block-causal geometry (causal to context, bidirectional
# within block), single-step masked drafting, drafter-only training.
#   uv run python src/train.py +experiment=baseline
defaults:
  - override /model: qwen2_0.5b

# own dir + filename: runs of different presets never overwrite each other's
# checkpoints (incl. last.ckpt) or TensorBoard logs
output_dir: checkpoints/baseline

train:
  variant: baseline_block_wise
  checkpoint_name: baseline-{step:07d}
